/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TimerPipe } from './timer.pipe';

describe('Pipe: Timer', () => {
  it('create an instance', () => {
    let pipe = new TimerPipe();
    expect(pipe).toBeTruthy();
  });
});
